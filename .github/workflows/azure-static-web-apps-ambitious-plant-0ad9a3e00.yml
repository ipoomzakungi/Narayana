name: Azure Static Web Apps CI/CD (disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Azure deployment disabled
        run: |
          echo "Azure Static Web Apps auto-deploy is disabled."
          echo "The rg-narayana-demo resource group and narayana-dashboard Static Web App were deleted on 2026-05-28."
          echo "Recreate Azure resources and restore this workflow before deploying to Azure again."
